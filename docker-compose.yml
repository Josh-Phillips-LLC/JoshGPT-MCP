services:
  joshgpt-mcp:
    build: .
    container_name: joshgpt-mcp
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - "${JOSHGPT_MCP_WORKSPACE_HOST_PATH:-.}:/workspace:ro"
    ports:
      - "${JOSHGPT_MCP_PUBLISH_HOST:-127.0.0.1}:${JOSHGPT_MCP_PUBLISH_PORT:-8787}:${JOSHGPT_MCP_BIND_PORT:-8787}"
